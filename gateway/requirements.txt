# Match the orchestrator's deployed deps so the in-process agent can reach
# MCP servers (over Streamable HTTP), AWS A2A agents (SigV4 via boto3),
# and Vertex AI Memory Bank / Session services.
google-adk[a2a,agent-identity]>=2.0.0
mcp>=1.0.0
google-cloud-aiplatform[agent_engines]>=1.70.0
google-cloud-bigquery>=3.25.0
google-cloud-logging>=3.10.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
boto3>=1.34.0
botocore>=1.34.0
