steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/<YOUR_GCP_PROJECT>/fsi-rm-liveapi-broker', '-f', 'bridge/Dockerfile', '.']
images:
  - 'gcr.io/<YOUR_GCP_PROJECT>/fsi-rm-liveapi-broker'
